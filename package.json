{
  "name": "ledgerdesk",
  "version": "0.1.0",
  "private": true,
  "description": "A grounded-answer platform over versioned knowledge corpora.",
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "corpus": "node --experimental-strip-types quality/gen/corpus.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.8.0"
  }
}
