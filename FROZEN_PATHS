# Frozen scope. A PR touching any path below must add or reference an accepted ADR (see adr/).
# This gate is necessary, not sufficient: a domain or corpus change that touches no listed path
# is covered by the PR template's domain-or-corpus ADR requirement, not by this file.
# On tag demo-w7 this file gains the src/ globs (application freeze mechanism).
migrations/
quality/schemas/
