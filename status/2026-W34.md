# Weekly status — 2026-W34 (week of 17–23 Aug 2026)

**Phase:** A — documentary baseline + wrapper start.

## Done this week
- Delivery repository initialised (now public); spine in place: board, weekly status, minutes template, ADR template, provenance, CI check (I0).
- Scope of the repository decided and signed (DA-10): clean delivery repo, offline design record stays offline.
- Documentary baseline merged (PR #1) and sealed with the immutable tag `baseline-v1.0` — **phase-A exit gate passed two days early**.
- Process gate live (I0b, PR #2): `adr_gate` in CI, `FROZEN_PATHS`, PR template, protection of `main` requiring both CI jobs; evidence pair red → green produced on a real frozen-path change.
- Data spine merged (I2 + I2b, PR #3): four deterministic migrations, nine-class ledger contract with chain monotonicity and truncate locks, sealed-manifest schema v1.1, deterministic corpus generator, thirteen database and generator tests green on two platforms (CI and a local PostgreSQL 16 container) after an adversarial review (33 fixes + 32 hardenings, six dated ADR amendments).

## In progress
- I3 app skeleton: auth, three roles, ticket CRUD, state machine — closing evidence: US-01 green + 3 navigable screens.

## Next
- I4 LLM gateway (before any agent) and I5 pure algorithm, per the critical path.
- Phase B opens Mon 24 Aug: end-to-end slice + recorded rehearsal (VC-04 gate Mon 25).

## Risks
- The end-to-end slice must run by the VC-04 gate or firewall/pilot work freezes — I3 is the long pole this week.
- Four owner decisions are pending with later deadlines (eval-outcome writer before I10; OOD probe identity before sealing; error-code language; review-batch options), none blocking I3.
