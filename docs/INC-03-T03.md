# INC-03 T03: bounded extraction — correction R2

Status: the bounded correction and its named local observations are recorded
below, for independent implementation review. **T03 is not accepted or closed.**
The bounded-extraction amendment to ADR-038 remains proposed. No publication,
real-data use, preparation, candidate constitution or T04 is authorized here.

Base: `a1a9175b89e22845a09463b8eb492fbcaf23babd`.
Branch: `card/INC-03-bounded-extraction`.
This revision supersedes the earlier report's unfinished correction list, not
its retained observations. Earlier failed executions remain failed.

## Actual delivered path and contracts

HTTPS reception preserves the original. Separately admitted internal dispatch
uses the fixed isolated worker and controlled channel. Raw and normalized
observations are conserved separately; acceptance and protected query each
require current authority. Receipt, original generation, attempt generation,
channel, normalized identity, staged output and accepted effect are distinct.
Known effects are queried, not executed again to recover a lost response.

The profiles remain `intake-worker/3`, explicitly selected
`intake-reception/2`, `intake-availability/2` and `intake-extraction/1`.
Historical `intake/1` intention identity and canonicalization are unchanged.
A populated historical intention is exercised before installing the additive
extraction schema, then recovered through v2. Actual SQL admission prevents
promotion during the prior response handoff. After promotion, v1 conflicts
instead of describing an unstarted job. The receipt/effect is not duplicated.

The metadata projection is a closed `view: metadata` response with job state,
receipt/original references and accepted result identity/effect only. It does
not materialize original, normalized, raw or resource bodies. Content requires
its own current authority. Original-body and extraction-body permissions are
independent. Hidden and absent results are compared after actual successful
population admission, with read counters, status, headers, body and the
declared finite timing detector plus a shifted control. This is not universal
timing indistinguishability.

| Area | Principal files |
|---|---|
| Contracts and mapping | `src/contracts/intake_extraction*.ts`, `intake_reception_v2.ts`, `intake_mapping.ts` |
| Current authority and execution | `src/server/intake/extraction*.ts`, localized `authority.ts` |
| Durable accounting | `src/server/intake/postgres/004_extraction_control.sql` through `007_extraction_capacity.sql` |
| Private storage, controller and channels | `ci/intake/extraction/` |
| Fixed native adapter | Pinned `workers/intake/extraction/`; unchanged by this correction |
| Executed references and faults | `tests/intake/extraction/`, individually retained source copies |
| Mandatory verification | `ci/intake_extraction_guard_checks.mjs`, workflow, independent wiring and result gate |

## Finite capacity and representation

| Boundary | Exact limit |
|---|---:|
| Original | 1 MiB |
| XLSX sheets / aggregate physical cells | 8 / 10,000 |
| Retained worker stdout | 40 MiB |
| Normalized artifact | 16 MiB |
| Per-attempt raw + normalized reserve | 56 MiB (58,720,256 bytes) |
| Aggregate extraction conservation accounting | 256 MiB (268,435,456 bytes) |
| Failure projection inside normalized allocation | 64 KiB |
| Private request/response frame | 80 MiB |
| Protected HTTP projection | 16 MiB + 64 KiB |
| Analyzer cgroup / V8 heap | 512 MiB / 128 MiB |
| Trusted private transport cgroup / per-process heap | 1 GiB / 256 MiB |

Swap is not added to either cgroup. The aggregate accounting is separate from
original storage, analyzer scratch space and OS disk capacity. It is not an OS
disk quota. The former 64 MiB aggregate was already exceeded by the measured
126,355,484-byte retained corpus. That corpus plus one new and one unresolved
56 MiB obligation requires 243,795,996 bytes; the finite 256 MiB realization
accommodates that measured scenario. It is not an unlimited budget increase.

The actual SQL attempt insert atomically reserves before protected reads or
launch. Pending, failed-unsealed and uncertain work retains its reservation;
sealed-before-SQL work remains charged. Staged durable bytes replace the reserve
once; repeated reconciliation does not charge twice. Failed conserved output
still counts. Existing history is not deleted to admit new work. Independent SQL
connections compete at the actual capacity-row lock, without relying on the
separate service single-live-job guard. Denied service admission adds neither
protected reads nor launches and leaves prior results queryable.

The valid 60,000-LF original now completes through the real worker, storage,
acceptance and protected query: 11,646,808 raw bytes, 6,845,205 normalized bytes,
18,492,013 conserved bytes. Compact text elements retain literal text and exact
byte/code-point ranges against the enclosing original/source identity. This
fixes the measured normalization defect without reducing the original bound or
increasing transport limits. It does not prove every admissible file completes.

The supervised output-limit experiment retains the permitted prefix and a
separate failure projection. `stdoutEncodingError` describes that retained
prefix; a supervisor cut can split valid producer UTF-8. `output_limit` remains
the primary cause. Actual formula/cache, hidden-sheet, context, locator,
record/column and multibyte-record checks reach conservation and authorized
query. Cached 24 is not recalculated to 25; missing and zero remain distinct.

## Associations, resources and distinct causes

Two genuinely received originals with equal visible text receive independent
jobs and channels. Six individually valid binding substitutions
(original/job/attempt/profile/destination/channel) reach the internal result
boundary with valid framing and recalculated integrity, then fail association
before cross-job acceptance. Both legitimate originals remain useful. These are
explicitly instrumented internal substitutions, not a claim that an untrusted
worker can forge the physical host channel. An alternate valid destination in a
negative does not authorize a new processing target.

The native adapters still do not discover nonempty resources or relations.
The labeled instrumented producer uses two real readable intact resources,
an independent operation reference and an indispensable relation. Both legitimate
selections are accepted. A wrong-but-intact association and duplicate identity
are rejected before resource-body reads. Treatment withdrawal also prevents the
metadata/resource access. Internal relation removal reaches the actual
persistence/projection and fails its independent expected result.

The resource negatives settle their own test state before the next case; a
capacity cascade cannot count as duplicate-ID discrimination. Each faulty
source copy identifies precisely one mutation. Useful baselines accompany the
negatives. No T04 preparation, constitution or native image-extraction claim is
introduced by this instrumented boundary.

Condition, coverage-limitation and incident omissions similarly reach accepted,
structurally valid projected data before failing independent expectations.
The labeled mixed-component branch conserves unreadable, route-unoffered and
technical-failure outcomes separately, alongside real useful extracted text
and an unknown unexamined remainder. Those injected component causes are not
reported as native adapter discoveries. Structural integrity is not an automatic
semantic-completeness oracle, and registering a package is not authentication.

## Authority, loss, isolation and recovery

The preserved authority run
`intake-extraction-2026-09-16T06-33-18-171Z-196605c4` failed:
17 TAP tests, 8 pass, 8 fail, 1 cancelled. Its 180-second parent expired.
Cleanup markers precede the first 503 in a restored-authority positive; subsequent
connection failures follow teardown. It is not evidence of eight new product
defects or a passing matrix. The finite matrix now has three groups:
before original read, before result read, and before protected effect. Each
exercises treatment, assignment, plan, support and faculty withdrawal with its
positive, zero-read/effect and historical-state checks. No deadline was enlarged.

Actual Chromium disconnection is distinct from logout/revocation in both
declared session modes. The session remains valid after the browser closes;
independent current processing faculty still controls dispatch. Removing that
faculty prevents new reads/attempts; restoring it permits useful extraction.

Real predecessor/current worker identities exercise delayed old completion and
duplicate current publication. The first generation ends with an observed input
interruption; its completion is real, but no successful old body is invented.
Actual bridge publication cannot replace the current completion, artifact,
accepted result or SQL history.

A labeled worker copy performs actual denied file read, write, subprocess and
network attempts; a separate actual nontermination reaches the unchanged
ten-second supervisor and observed closure. The positive still extracts useful
text. Denied network routing is not inferred merely from a configured flag.
These finite probes do not establish a universal hostile-code sandbox.

Real service death at seal/SQL and staging boundaries, HTTP restart and private
raw-seal interruption preserve identities and charges. Actual post-launch host
controller death leaves uncertainty with the exact launch identity; later
dispatch, retry and acceptance cannot fabricate termination. Replacing an
output participant with an unresolved sealing phase preserves the unclosed
journal and refuses adoption. Its analyzer had already closed: this is not a
claim of killing a running analyzer during that replacement. Fail-closed
uncertainty is not automatic recovery, high availability or a successful stop.

The populated restore now conserves a completed text result and a genuinely
failed CSV extraction, actual original/output bytes and retained historical
identities. Current authority and nonrestored control remain outside the archive.
Rehashed altered rows, absent output and wrong archive associations are rejected.
Triggers stay enabled. Separate restored bytes remain useful after old live
bytes are corrupted. Its bounded archive is not universal backup capacity.

The qualification oracle and historical memory evidence are retained from
[the accepted exclusive L03 reference](L03-exclusive-reference.md).
The default singleton profile, fixture and termination oracle are unchanged.
Its positive records exclusive
hierarchical memory pressure and victim counters; external/watchdog controls
do not qualify. **This is not a new qualified execution of the T03 image.**
Windows/Docker Desktop does not satisfy the required same-host Linux/systemd/
cgroup-v2 reference. That later image-specific qualified execution remains an
explicit environment dependency at the authorized publication stage. Exit 137,
sampled peaks and configured limits are not substituted for it.

### Image-specific memory verification connection

The existing worker verification now accepts the explicit `--qualified-memory`
option, only with `--group worker`. The ordinary useful worker cases and the
memory, external-kill and watchdog controls use the same built extraction image.
The additional `t03-init-probe/1` execution profile retains `--init`, identifies
the init process and its sole Node child separately, and checks the trusted
read-only probe mounts, exact file bytes and effective controls before release.
Missing proof, failed export or unconfirmed cleanup cannot qualify the run.

The existing `intake-extraction` CI job requires this option. Its public evidence
includes a closed `t03-memory-proof/1` record that can be replayed through the
same termination oracle, rather than only a success flag. Directed local checks
exercise the connection, rejection cases, export and cleanup with synthetic
observations; the unflagged local worker run remains explicitly unqualified.
This connection is implemented and locally checked. **Physical qualification of
the current image is still pending** the authorized same-host Linux run; neither
the model observations nor the historical execution close that requirement.

## Identified correction executions

All IDs below are under `test-results/intake-extraction/`. TAP totals include
identity controls and parents, not distinct requirements. A later log retrieval
does not create another execution. Raw/private observations are retained locally.

| Group | Run after prefix `intake-extraction-` | Observed TAP |
|---|---|---:|
| Dense text and bounded output | 2026-09-16T05-46-20-251Z-98dc5d93 | 5/5 |
| Affected full formats | 2026-09-16T05-54-26-658Z-88125f0f | 19/19 |
| Authority: original | 2026-09-18T15-53-17-677Z-48dcb5d0 | 7/7 |
| Authority: result | 2026-09-18T15-55-00-477Z-a0819c29 | 7/7 |
| Authority: effect | 2026-09-18T15-56-38-540Z-7ade81b7 | 7/7 |
| Historical compatibility/race | 2026-09-18T16-01-33-075Z-cd3a370a | 4/4 |
| Resources including withdrawn treatment | 2026-09-18T16-02-14-756Z-86e272a4 | 7/7 |
| Old/current completion | 2026-09-18T16-03-16-339Z-b7b08444 | 4/4 |
| Two-original associations | 2026-09-18T16-04-05-271Z-909d2136 | 10/10 |
| Physical browser disconnection | 2026-09-18T16-09-53-800Z-13dcc377 | 4/4 |
| Effective containment and timeout | 2026-09-18T16-12-24-627Z-2754ce39 | 5/5 |
| Populated privileges/downstream | 2026-09-18T16-19-49-758Z-453c06b1 | 4/4 |
| Aggregate capacity and metadata | 2026-09-18T16-20-33-982Z-b87a1e61 | 11/11 |
| Changed storage recovery | 2026-09-18T16-22-55-113Z-51358d81 | 7/7 |
| Changed populated restore | 2026-09-18T16-24-09-011Z-ada00f82 | 10/10 |
| Actual controller loss | 2026-09-16T06-03-51-808Z-6c43698e | 5/5 |
| Unresolved private replacement | 2026-09-16T06-08-45-585Z-4eb6eeef | 5/5 |

The guards are recorded separately in
`guard-controls/resources-2026-09-18T16-25-20-193Z-70e7a405-6d29-48e6-bd89-19e2275e2198`
and `guard-controls/formats-2026-09-18T16-32-59-944Z-d3df1d91-7b10-4023-9dc9-a11f6fb7a327`.
Three useful baselines pass. All nine directed mutations fail their named
assertion: three resource/association, three condition/coverage/incident and
three XLSX cache/hidden/context mutations. The failed run manifests stay failed.
Qualification also requires exact source mutation, normal runtime completion,
zero cancellations and owned cleanup; arbitrary nonzero exit is insufficient.
The stricter final qualifier was applied to these retained executions without
claiming another worker run.

Earlier failed source copies with an unintended extra unused XLSX fault are
retained, not used as single-change evidence. Failed browser/containment/privilege
test preparations remain recorded: missing login intent with an unclosed test
browser, probe diagnostics in a closed metadata field, and unavailable-route/
fixture mistakes. The corrected tests neither weaken the production contracts
nor hide those failed runs.

The final coupled service run `2026-09-18T16-44-02-869Z-f03a6950` also remains
failed. Three earlier stopped obligations plus the retry predecessor consumed
four 56 MiB reserves; the next reserve correctly exceeded 256 MiB and returned
429. Later capacity/admin-fault errors are consequences, not successful retry
observations. E04g/h/i now execute unchanged in a mandatory separate `retry`
cluster. The service retains its actual four results/seven attempts; the retry
cluster requires three results/five attempts. No history is deleted, cap raised
or acceptance assertion dropped to accommodate a larger test dataset.

Final unit/schema, service/restart, eight-entry clean composition and repository
checks have their own uniquely identified manifests. They must be checked
against the frozen source and any later delta; the individual rows above are
not a substitute for those final consumers. Coupled R1 access suites remain
21/21, 25/25, 27/27 and 26/26 in the retained execution; its overall manifest is
failed because the subsequent journey hit a source-composition mismatch.
The separate stable journey passes 28/28. Reuse requires unchanged actual access,
UI, contract and test consumers, not merely a frontend/backend label.

## Original acceptance reconciliation

| Rows | Concrete observation | Qualification retained |
|---|---|---|
| T03-01 | Populated v1 recovery through v2; actual promotion/handoff contention | No identity migration or effect replay |
| T03-02/10 | Two actual equal-text originals, six internal binding substitutions, legitimate alternative | Instrumented association boundary, not forged host-channel authentication |
| T03-03 | Strict vectors, real invalid originals, supervised UTF-8 output cut | Retained-prefix diagnosis is not producer blame |
| T03-04/05/06 | Actual CSV/XLSX limits and at-limit positives; literal cache/context/locator/hidden mutations; dense text fixed | Bounded profiles, not universal format or semantic coverage |
| T03-07/20 | Effective denied accesses, supervised timeout, output limits, cleanup and finite SQL conservation | Qualified L03 mechanism reused; current T03-image memory qualification remains pending |
| T03-08/09 | Native partial/failure cases, labeled mixed causes, internal omissions and indispensable relation | No automatic semantic oracle or native resource extraction |
| T03-11 | Two real intact resources, independent association, duplicate zero-read rejection, legitimate second selection | Instrumented producer; no T04 preparation |
| T03-12/13/14 | Fifteen withdrawal cases at three actual boundaries; both session modes and physical browser loss | R24 is separate; disconnection is not revocation |
| T03-15/16/19 | Atomic claim/reserve, retries/stop, old/current completion, real controller and unresolved-private losses | Uncertainty fails closed; no automatic failover |
| T03-17/18 | Lost reply, same known effect, current query denial, interrupted seal and populated restore | Exact synthetic cut and finite archive |
| T03-21 | Useful zero-body metadata, original/body independence, admitted population neutrality and shifted timing control | Finite detector, not universal side-channel proof |
| T03-22 | Retained seven writer/effect controls and seven actual expiry violations | R24 remains observed nonconformity |
| T03-23 | Actual extraction with unchanged downstream state, valid unavailable routes, populated own/cross-role SQL probes | No preparation/candidate/publication producer |
| T03-24 | Mandatory finite groups, named negative qualification, public evidence allowlist, coupled checks | Local author evidence; remote CI and independent R2 review not performed here |

## Reproduction and review discipline

Use Node 22.16.0 and the pinned Linux Docker images. The actual runtime claim is
`test-results/intake-t02/.active-run`. Execute runtime groups consecutively;
do not remove another run's claim or edit included source during a consumer.

```sh
node --experimental-strip-types ci/intake_t02_check.mjs --group extraction --extraction-cases <group>
node ci/intake_extraction_guard_checks.mjs --suite resources
node ci/intake_extraction_guard_checks.mjs --suite formats
node --experimental-strip-types ci/intake_extraction_check.mjs --group units
node --experimental-strip-types ci/intake_extraction_check.mjs --group schema
node --experimental-strip-types ci/intake_extraction_check.mjs --group composition
```

Runtime groups are the exact names in the workflow and runner: `service`,
`formats`, `budgets`, `retry`, `restart`, `storage-recovery`, `restore`,
`authority-original`, `authority-result`, `authority-effect`,
`compatibility`, `capacity`, `extraction-privileges`, `resources`,
`associations`, `lineage`, `browser-disconnect`, `containment`,
`private-loss`, `controller-loss`, `semantics`, `mixed`,
`format-negatives`, `fencing` and `temporal`.

CI retains the eight earlier jobs and has six required bounded extraction jobs,
each with a 20-minute cap. The independent reading gate requires all ten test
producers. Missing steps, skipped jobs, retries and relaxed assertions cannot
replace a required result. Local durations do not certify remote timing. The
public exporter constructs a closed summary; private bodies, commands and raw
sidecars are not uploaded by that exporter.

Retained test-result copies are archives, not active modules. The focused seam
repair excludes only the root archive tree; actual and misleadingly nested
unauthorized SDK imports still fail directed tests. Source/evidence identities,
cleanup and unchanged-consumer comparisons remain required at handoff.

R24, BROWSER-I03 and BROWSER-J19 remain open. Their green surrounding tests do
not establish a fix. This report does not accept ADR-038, authorize real data,
start T04 or replace the outstanding independent implementation review.
