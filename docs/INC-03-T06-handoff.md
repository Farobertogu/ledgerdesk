# INC-03 — concrete editorial handoff

This is a synthetic produced example and a downstream contract boundary, not approved material or authorization to start INC-04. Evidence run: `intake-extraction-2026-09-22T13-34-21-151Z-02fcc7cc`. The private retained `runtime/whole-journey.json` has SHA-256 `824032c526672e200d051aaa1958edd0e1b8d5ccd7244291efba3947a3bb3c67` and contains exact original bytes, preparation bytes, descriptor, differences, candidate, proposal and constitution outcome read before cleanup. It is not an operational database backup or a public export.

## Exact produced references

The bounded synthetic T06 scope was accepted on 23 September 2026 after independent review. This accepts the measured intake composition, not the candidate's editorial content. Hosted publication is recorded separately in the [T06 delivery](INC-03-T06.md); no acceptance statement here starts INC-04.

| Object | ID / revision | SHA-256 |
|---|---|---|
| Original | `11d119e7-7e8c-43cd-a20d-6af0c64d979e`, generation 1, 99 bytes | `4a08f8b7c959e0b3f3f8fa8a0797e66f8936a7cd79327615f38d1dbcb8bff7fd` |
| Receipt | `33aa4ff5-d604-4459-b792-e4254981b892` / 1 | `6b4ad94313a50c5d461720662aa56f21a1086e01db2760195ea80cce0e61c733` |
| Accepted extraction | `8502cbb2-a97f-476e-a51e-5b0d42a8adf1` / 1; job `fc5e258f-ce3d-4738-ac23-5ba2b3b27351` | `e40666114488c791fc94273fbb50cbee833a8892dcfe3d6e234c42d344a2b822` |
| Extraction source reference | `d994a20f-5840-4a87-805b-ad309cb816e8` / 1 | `38c5b57617b9bc737089da9c49e33e50b6caea04f6e48134dae12f4ca720acfd` |
| Preparation descriptor | `e55c743a-bcda-44c6-855a-87d937d4f004` / 1 | `53e302ac12a8ff8e1965502b814ef41c122118c4fc93589a55748c6c302be038` |
| Exact preparation payload | `e16b3d12-8cf0-4881-aa38-767e72d0c506`, generation 1, 2682 bytes | `f3336755b7e5e5f184bededd1db6782d118aec95eec5aed86b85daed9467fde7` |
| Difference | `e55c743a-bcda-44c6-855a-87d937d4f004:r1:difference-0` / 1 | `c46df9b6adf0511f8341ac9846bc684d146988763ea5bcf6609624888de2e6ba` |
| Proposal | `0e45c71f-3fc3-43cd-8864-a9664bed7bb0` / 1 | `0bfcbac61e6109a73c76f0f54a67601395334b202969e4d6da170871128c9f67` |
| Constitution effect | `f18ddddd-5db7-4c58-a30a-9a5b559c494e` / 1 | `dd8dacde3dc18093621f94623c585960ebc986adbdf7b472d1b6ea2754601558` |
| Candidate | `7282b350-754d-4a71-a74b-0c9b88703e08` / 1 | `113c5dbe0d1cc1ffa0e5f9948ddcf1333d31c144d638149dd305d1e91dfb8853` |

Digests identify different representations. In particular, a descriptor/reference hash is not necessarily the stored payload-byte hash. The extraction source artifact is separately recorded as generation 1, 1127 bytes, byte hash `2a45690870d76f98b3b33d8dcb3fc91bd158cc1d6495bde612b32b0f721f862d`; its exact reference is retained, but this handoff capture does not claim to archive the entire worker raw-output tree. The original and preparation bodies are retained exactly, not reconstructed from screenshots.

## Content, classification and differences

The selected unit is `selected-material` with elements `line-1`, `line-2`, `line-3`. Its original is:

```text
AZ-17
For procedure AZ-17, receipt Q is required.
Under condition Z, receipt R replaces receipt Q.
```

The human correction adds `Human annotation: synthetic only.` to the first element. Exact original byte/code-point ranges are 0–6, 6–50 and 50–99. The changed first element keeps its antecedent and explicit difference; the added annotation is not falsely presented as literal original text. The difference records before/after exact references, the synthetic actor, method `correction`, transformation `cleanup`, affected element/condition and the reason: `Explicit human preparation retains the source and declares its applicable condition.`

Classification is a human proposal: function `normative`, basis `non_authoritative_reference`, scope `situated`. Examination is `classifiable`, with the explicit statement that it is not automatic semantic validation. The condition is `Under condition Z, receipt R replaces receipt Q.`, scoped to `AZ-17`. The proposal selects all three named elements and the candidate retains this condition and exact preparation reference.

Coverage does not become unlimited: `inventory: unknown`, `current_use: not_evaluated`, `complete_source_claim: false`, component fidelity `unchecked`. The source component has `syntactic-profile-only`, `raw-properties-retained-not-interpreted` and `semantic-fidelity-unverified` limitations. This example has no resource bodies or explicit inter-element relations; it is not evidence of native visual extraction or every semantic dependency.

Identity is explicitly declared `distinct` with target `new`, not inferred from its content hash. The stored candidate state is `candidate`; zero material rows exist after constitution. The example has no block and no relationship. Separate measured identity/unit cases cover relationship, collision, unresolved possible duplicate, zero results and multiple independent units; those effects must not be fabricated onto this single-candidate example.

## What the next increment must preserve

1. Resolve exact immutable preparation/proposal/version references, not “latest”. Preserve original lineage, differences, coverage and conditions even when a current query no longer discloses them.
2. Perform current server-side admission for each new protected read and effect. An identifier, hash, historical receipt or copied evidence package grants no authority and does not authenticate provenance by itself.
3. Keep human classification/examination distinct from approval. Approval, publication and corpus-read eligibility need their own acts, authority, evidence and acceptance tests.
4. Do not merge a related receipt into a new version, overwrite an identity collision or silently resolve a possible duplicate. Preserve zero/one/multiple/blocked dispositions and the existing adjudication vacancy.
5. Carry R24's observed temporal nonconformity, the partial same-original fragment lineage, unmeasured selector-specific expiry, unsupported formats and semantic uncertainty forward. No real-data use is authorized.

The cluster that produced the example was deliberately removed by its owner. Review can use the conserved bytes/rows and source/run manifests; reproducing the command creates new synthetic identifiers, not the same database identity. A downstream operational import or migration is not implemented or authorized by this handoff.
