# INC-03 T02: durable reception and protected originals

## Status

The finite implementation qualification and mandatory CI wiring are delivered
for technical review on the local reception branch. No commit, publication,
remote CI result or acceptance is recorded here. The localized ADR-038 amendment
remains proposed. The latest qualification section below and the coverage
register supersede the pending-work status of the earlier checkpoints, without
rewriting their source identities or results. R24 and the exact same-original
fragment-provenance qualification remain explicitly open.

The implemented path is an explicit synthetic configuration of the existing
access terminal, with PostgreSQL 16 and separate private storage/verification
processes. Next is not an original proxy. Nine receipt/query operations have
handlers; extraction, preparation, candidate constitution and editorial work do
not. See [the contract](intake-contract.md) for exact operation/status semantics.

## Component map

| Component | Responsibility |
|---|---|
| src/contracts/intake_reception.ts | Closed reception, availability and problem representations; bounded dispatch inventory |
| src/server/intake/authority.ts and protocol.ts | Current server-resolved binding/session/treatment; exact transport and canonical command |
| src/server/intake/postgres/001_reception.sql and 002_data.sql | Additive control/roles and durable reception, intention, artifact, receipt/work and evidence records |
| src/server/intake/postgres/003_private_fence.sql and private_phase.ts | Durable private work, guarded source changes, terminal completion and fresh source epoch |
| src/server/intake/service.ts, reception.ts, stream.ts and terminal.ts | Actual reserve/upload/finalize/query/resume/cancel/original producers and protected handoff |
| ci/intake/reception/ | Contained object service, minimum verifier, phase journal and narrowly owned recovery helpers |
| tests/intake/t02/ | Fixed synthetic references, real HTTP/SQL/object/browser observations and directed faults |

The phase migration also guards current access authority sources. It therefore
requires the access regression suites against the actual installed migration,
not merely separate tests of the new namespace. The source inventory and its
effective-role observations distinguish table/column privileges, empty statement
trigger probes and populated mutation probes; their counts are not interchangeable.

## Current executed evidence

Runs below live under test-results/intake-t02/. Each directory preserves a source
manifest, actual commands, observations and owned-resource inventory. These are
local observations, not a remote CI result or an independent acceptance verdict.
The listed counts include the containing Node test where applicable.

| Run | Observation and exact scope |
|---|---|
| intake-t02-2026-09-13T02-32-25-529Z-0dd00be3 | Browser 9/9: real invited session, availability, exact 17-byte upload, durable receipt before POST response, exact original download, known-effect recovery after loading withdrawal, query refusal after query withdrawal; 12 owned resources removed |
| intake-t02-2026-09-13T02-37-45-029Z-21382002 | Actual source-fence migration installed alongside access runtime 21/21, invitations 25/25, authenticated reading 27/27, administration 26/26 and whole journey 28/28; seven resources removed |
| intake-t02-2026-09-13T02-42-49-821Z-24680f65 | Reception DTO/minimum-form/bridge/request-observer units, intake/access boundaries and application types; see individual command records rather than one combined property count |
| intake-t02-2026-09-13T02-03-35-405Z-8ee985ce | Three actual original/record/denied GET windows, 10/10 including setup; eight named independent checks reuse those three windows, not eight separate executions |
| intake-t02-2026-09-13T02-04-44-128Z-7da4727c | Captured-source missing-read-observation mutation fails the exact original real-read requirement; 8/10, not a passing baseline |
| intake-t02-2026-09-13T02-05-36-846Z-1b3b49e2 | Captured-source delivered-byte mutation fails the exact-byte original requirement; 8/10, not a passing baseline |

Browser source manifest SHA-256:
72adfb706b7c3a29693f944a80ff8e9ec0dea1eb93907ca78c1578cb6fd244db.
Coupled-source manifest SHA-256:
e7dd8f9d322ab2b10f74e86953e153edde63faa1aba3b0b3a8158cd6fb4756d7.

The browser's pause observes committed delivery evidence with prepared status
200 and the actual receipt, an idle SQL backend without an open transaction,
the outstanding browser request, and no POST response/data in Chromium's network
events. OPTIONS preflight is retained separately. The later original response is
compared byte-for-byte with the independently fixed original. This does not prove
full temporal conformity, the future upload UI or a browser's durable storage.

An ordinary Next production build also completed locally during this pass. It
is distinct from the isolated service/browser image and does not activate intake
handlers in Next. No remote build or final-source CI result is claimed.

## Failures retained, not relabeled

- Browser driver preparation runs at 02:27, 02:28 and 02:30 retained, respectively,
  a missing test login intention key, an observation that confused preflight with
  the protected POST response, and an ambiguous column in the test withdrawal
  statement. The later corrected driver uses the actual session cookie/profile,
  separates methods/request IDs and qualifies that test SQL. No producer protection
  or timing assertion was relaxed to fix these test setup defects.
- The 02:20:40-752Z-79dabe7c canonical integration run retains an actual checker
  rejection of prior metadata evidence whose reception ID is legitimately null.
  Finalization creates and then recovers the same effect; the prior row was not
  rewritten to make the checker pass.
  A focused review also identified an attribution defect in that collector: it
  assigned an early metadata row the later connection's idle sample. That old
  capture is retained, not corrected retrospectively. The current collector
  samples the row's own backend, and never turns an absent sample into `false`.
- The 02:23:30-855Z-4a87d539 receipt integration run retains the separate earlier
  upload/seal lineage and the later finalization. The checker demanded a seal
  from the finalization call itself. That association is not fabricated. These
checker integration findings remain separate from producer behavior and await
  the identified correction and its real rerun.
  Its earlier upload sidecar did not retain all the required client/phase
  witnesses, so it is not upgraded to a complete prior-upload proof.
- The 03:02:18-902Z-a53c528e phase capture stopped because the observation helper
  tried to open an event journal before the participant's first operation had
  created it. The failed commands and exported artifacts remain retained; all
  twelve owned resources were removed. Initial absence is now explicit, while
  the post-operation journal is required to exist. No producer protection was
  relaxed. The intermediate 03:04:31-615Z-fa94290c capture remains separately
  identified and does not inherit the additional captures of the next run.
- The earlier missing-stop counterexample and all fault runs remain retained.
  Later fencing evidence is source-specific; passing counts alone do not erase
  the original counterexample or establish universal protection.

## New phase/lineage capture

The local run `intake-t02-2026-09-13T03-08-45-599Z-9ebe1eb0` completed its five
reported tests: three setup checks, one real lineage scenario and its parent.
This is not five independent lineage experiments. Its source manifest digest is
`ef28358c6af15e7fa0ae32ad8672a1b49b37178e261ea00edd041a825989d52f`.

The actual scenario retains a binary upload and two finalization calls. Both
finalizations return 200 with one receipt and one unstarted job. Metadata
connections 238 and 240 have their own independent idle samples; the later
selected connections are 239 and 241. The read-admission record used by upload
verification is qualified by the persisted private phase, actual allowed plan,
source/actor/original and seal request ID. It is not general write authority.

The capture contains fixed pre-upload expectations, real client/server socket
observations, separately retained sent/consumed/physical bytes, the durable phase
observed before seal dispatch, matching raw physical events, participant closure
observed before the upload response, and a later finalization before snapshot
with an exact sealed attempt but no receipt/job. References for finalization are
fixed before that call. Raw SQL, transport and request records remain available;
the compact interface uses explicit file digests and JSON pointers to them.

These are producer-side observations and mapped `3.3` packets, not an independent
checker acceptance. The previous rejected packets are unchanged. The new hooks
and private-port observation require their coupled regression pass; ordinary
source correctness, complete T02 coverage and temporal conformity are separate
obligations. The coupled run
`intake-t02-2026-09-13T03-10-12-252Z-12dc8106` subsequently passed runtime 21/21,
invitations 25/25, authenticated reading 27/27, administration 26/26 and whole
journey 28/28 with the actual fence migration installed. Its source manifest is
`dc8d1555d1924d90975c7e0c2fdb63a65bbd587316113f483b0f47cb33964f8b`;
all seven owned resources were removed. This is a new coupled run, not an
extension of the earlier browser result.

### Identified follow-up captures

The `03:08` packets were subsequently rejected at the independent catalog's
`input.fixture` check: their known 17-byte BOM original was named `original.bin`,
not the catalog's `bom.txt`. This was an interface identity mistake, not observed
byte corruption or a completed evaluation of their internal lineage. Those
packets remain unchanged. The producer fixture was corrected and the complete
real scenario was executed again; no old observation was rewritten.

The new run `intake-t02-2026-09-13T03-18-09-653Z-3700655b` completed the same
five reported tests, retaining its three distinct actual calls and the complete
prior upload. Its source manifest digest is
`697eec1276362da49aeded6edc3fc5d32a967910998e595cf88c6650b6db6dea`.
The raw observation digest is
`199e779f395262889bf5e3979a9c79281cfa75c8079025998ee086d26c0431dc`.
All twelve owned resources were removed.

The separately reported checker R5 run over these exact packets accepted
`IC05/receipt`; `IC04/compatible` stopped at `prior.distinct-window-classes` because
that guard only admitted the receipt case. The prior upload window is retained
in both packets. This is a confirmed checker composition constraint, not an
observed metadata/seal failure, and receipt checker acceptance is not an
independent audit of the producer. No service rerun or observation suppression
is justified merely to bypass that guard.

The released R6 checker was subsequently composed locally against both exact
03:18 packets: IC04/compatible and IC05/receipt both completed successfully.
All 24 immutable checker sources were verified against release manifest
`82d8554eb6c72f02245fb152b2f4a358897d49aa94ff0d075ce6a5f10b1e1b4a`;
the original inputs and replay copies remained unchanged. Its 6,502 and 4,609
internal assertions are not distinct product guarantees. This replay closes
the localized composition rejection; it is neither a new service execution nor
independent audit of how the producer observations were collected. The earlier
R5 result remains part of the history.

The immutable R4 checker was also exercised locally against the real original,
record and denied GET windows in
`intake-t02-2026-09-13T03-18-36-349Z-d9744ead` (10/10 including setup; twelve
owned resources removed). Source manifest:
`e0b40bea90dd4098796613df9cfe45b25fb73d583b0d0c6088d28d79ab1136ff`.
The pinned checker release has twenty-one files and manifest digest
`8b09976d67a79a22899f889b35f7fa3aafb13f3d56cd89f440d2761dac15cb36`.
It does not accept interface 3.3 and is not credited with checking the new
phase-lineage packets. Its completed-delivery association correction was
separately reviewed; that result does not close the remaining producer cases.

Affected-consumer runs after the new hooks passed the ordinary runtime (31/31),
browser (9/9), lost seal acknowledgment (23/23), lost private-commit reply
(23/23), and fresh continuation (24/24). Their respective run suffixes are
`03-14-26-497Z-2fdfa5df`, `03-15-07-325Z-fa038ad7`,
`03-15-33-290Z-5914bd72`, `03-16-07-998Z-ea2009f3`, and
`03-16-43-062Z-96d91a84`, each under the `intake-t02-2026-09-13T` prefix.
All twelve resources of each run were removed. These are coupled reruns,
not a claim that the full recovery matrix ran again on the later source.

## Reproduction and remaining composition

### Actual damaged/missing originals and process restart

`intake-t02-2026-09-13T03-51-06-528Z-c2f8fd19` passed 5/5, including identity
setup and its parent. Source manifest:
`cbb269e4ec94f27f98166fd9d3134d79ebc60719436093d4c9534e167e2c7c5d`.
The test-only storage actor preserves each exact original before making it
unavailable or installing a same-length corrupted counterpart. Record queries
retain the historical effect without any new object event. Original queries
reach the actual failed open or the actual 17-byte corrupt read, return 503 and
never manufacture original bytes. Receipt, work and attempt records stay equal.

The restart scenario closes the actual application process, observes its exit,
starts a distinct process with a new admitted control incarnation and compares
the exact durable effect and original across restart without reseeding. A
control/namespace-generation mismatch remains an explicit 503/no-object-access
negative before the namespace is properly admitted. This is not a production
failover policy or a substitute for the separately tested in-flight fencing.

Two failed preparations are retained: `03-45-37-153Z-14970dc1` changed the
control generation but omitted its corresponding namespace admission; the
service correctly rejected the inconsistent setup. `03-48-01-868Z-722cca51`
hit the host command-line bound while returning a large observation inventory.
Administrative replies now travel as files with independent length/hash checks
and atomic publication, not large command arguments. The affected run records
remain failed; per-case observations are preserved before later scenarios.

### Actual limits and read provenance

`intake-t02-2026-09-13T03-37-20-816Z-e5df87cd` passed 15/15 including its parent.
It exercises real HTTPS/SQL/private storage: an empty original, exactly 1 MiB,
invalid/truncated UTF-8, PDF/empty ZIP presented as XLSX, ZIP traversal and
member/expansion bounds, and an intact workbook counterpart. Minimum recognition
never claims sheet/cell extraction. The original and command limit positives
preserve their bytes; oversized declarations and five real TLS framing/media
negatives do not create staging or receipts. The oversized upload case is an
early declared-boundary rejection, not proof of every possible streaming overrun.

The initial run `03-29-06-804Z-bcb85bac` and diagnostic run
`03-31-02-253Z-3ca7d8af` failed the zero-read assertions. The observer wraps every
request `read`, including Node's automatic discard after a response; labeling
all of these as explicit application consumption was too broad. Actual Node
22.16.0 stacks in the diagnostic run distinguish `flow`/`resume_` from
`collectCommand`/`uploadOriginal`.

The opt-in boundary diagnostic preserves all raw read events, hashes and counts.
It qualifies native discard only after an observed `resOnFinish` call to
`req._dump`, with the matching internal flow, dumped state and no data listener.
An application-issued dump cannot acquire that attribution on a later repeated
call. Unknown/unqualified reads still fail zero-consumption assertions. The
contract's boundary is explicit application consumption and downstream capture,
not zero TLS/Node-buffer reception or zero transport discard.

The directed captured-source mutation in
`intake-t02-2026-09-13T03-35-13-453Z-f0b4aa19` reads one actual byte before intake
admission. It fails precisely the oversized command scenario and its parent
(13/15): status remains 413 and new receptions remain zero, but explicit
consumption is 1 instead of 0. The live observer controls also exercise explicit
reads and application-issued discard. The diagnostic is disabled in the earlier
phase-lineage packets; their identities and historical counts are unchanged.
The intermediate `03-34-17-026Z-4ff221f0` clean run is retained separately.

### New temporal counterexamples

`intake-t02-2026-09-13T03-40-54-441Z-150d0623` completed 10/10 reported tests:
identity setup, four positive/expired pairs and the parent. Source manifest:
`df2b7c9ccf974ad4400476ac1541ffdc4034a2d8529c95e67846e6abe0eea73f`.
Each actual operation selects a new immutable treatment revision before starting.
The expiry run pauses after the relevant actual clock evaluation, observes SQL
time independently, passes the deadline without an authority/control writer,
and resumes. Control identity and epoch are compared across that window.

| Protected point | Observed after expiry | Interpretation |
|---|---|---|
| Reservation commit | Reservation persists; response is 404 | Refusing disclosure does not undo the committed effect |
| Initial private dispatch before capture | 19 bytes explicitly consumed; response is 404 | The capture observation is not a claim that the broker appended them |
| Receipt commit | Receipt and job persist; response is 404 | Historical existence and current disclosure remain separate |
| Original handoff | 19 exact bytes delivered with 200 | Actual delivery occurs after the observed deadline |

All four are recorded temporal violations, not unproved assumptions and not
passing conformity claims. The experiment stores the observed property instead
of supplying conformity as an expected result. Its four before-expiry controls
reach the real effects. These source-specific observations extend the open R24
limitation; they do not excuse writer-fencing, authority or isolation defects.

Use Node 22 and the declared isolated Linux Docker environment. Do not supply
historical database URLs, mount the Docker socket into a service, modify host
certificate trust or reuse another run's writable directories. Run one T02
resource-owning command at a time. A missing prerequisite stops that experiment.

```text
npm run test:intake:reception:unit
npm run test:intake:reception:service
npm run test:intake:reception:recovery
npm run test:intake:reception:browser
npm run test:intake:reception:lifecycle
npm run test:intake:reception:bounds
npm run test:intake:reception:temporal
npm run test:intake:reception:integrity
node ci/intake_t02_check.mjs --group fence-coupled
node ci/intake_t02_check.mjs --group phase-lineage
node ci/intake_t02_check.mjs --group boundaries
```

The recovery command sequences the declared SQL-source, lost-process/connection,
lost-acknowledgment, commit-channel, continuation, queued-channel and restore
cases. Its per-child source and resource manifests remain the evidence; common
setup repetitions are not new independent guarantees. The matrix is being
measured before the mandatory CI composition/budget is finalized. Its first
complete local execution, `suites/recovery-2026-09-13T02-43-04-993Z-7ea71be3-b1d1-4b3a-8b34-08c7f2442d7b`,
completed all thirteen groups in 489,792 ms, with each child's resources removed.
It predates the phase-lineage observation hooks and does not replace the
subsequent affected-consumer checks. This is not a complete remote-CI timing.

The subsequent 21-group verification matrix completed in **856,099 ms**:
`suites/verification-2026-09-13T03-54-53-854Z-42fd9f80-e426-44d4-af84-635f07fcab21`.
Every child completed, reconciled its owned resources and matched the common
194-file source set; the coupled access child additionally identifies its wider
composition. Explicit fault-copy transformations compare against their recorded
original source hash. The matrix contains units, runtime, boundaries, temporal
observations, integrity/restart, browser, private-phase lifecycle, coupled access
and the thirteen recovery groups. It does not include every obligation in the
approved acceptance map or the whole existing mandatory reading job.

## Finite exit register

The following were acceptance gaps at the preceding checkpoint, not new
features. A missing test is an unmeasured risk, not an observed product failure.
Each item stops when its named observations and required discriminating control
are present; optional harness improvements do not reopen the complete matrix.
The full obligation mapping is in [the coverage register](INC-03-T02-coverage.md).

| Item / approved requirement | Exact outstanding work | Exit observation |
|---|---|---|
| P01 — authority and disclosure; T02-01/06/14/17 | Real administrator/grant-only refusal; wrong scope, wrong purpose, missing catalog, removed treatment; receipt-only original refusal; hidden profile row omission. Keep an actual authorized counterpart for each changed condition | Intended authority check reached; no protected body open/capture when forbidden; no hidden JSON row/count; normal permitted reception/read still succeeds |
| P02 — finite bounds; T02-03/09, control table | Actual slow/no-progress sender, one-active-upload refusal with query progress, three-attempt boundary retained across restart, 32-reception race and 64-MiB reserved-byte boundary. Distinguish parser framing from actual application read/capture limits and reserved bytes from physical storage | Measured deadline/bytes/queue and durable quota/generation observations; no oversubscription or old writer revival. A transport/parser refusal is not mislabeled as an application overrun detection |
| P03 — neutral timing; T02-18 | Real receipt, operation lookup and original negative pairs: absent versus other principal, plus owned record without whole-original faculty; independent original positive; existing finite timing protocol and +100-ms shifted control | Equal public status/body/header/metadata, zero forbidden storage access, original positive exact; observed timing signal and shifted-control discrimination retained. A detected signal requires a targeted correction, not a looser detector |
| P04 — effects/cancellation; T02-08/20 | Two legitimate principals using the same client key; committed-receipt/unstarted-job stop, repeated stop, stale revision and unrelated-principal refusal | Distinct permitted namespaces, unchanged prior receipt, job stopped without execution/deletion, exact conflicts and neutral denial; no callback resurrection |
| P05 — minimum directed producer mutations; T02-02/03/04/05/07/08/09/10/14/15/17/19 | Complete the declared mutation ledger: pre-capture gate, actual digest, byte cap, type acceptance, generation/fence, payload/unique effect, sealed-without-commit, whole-original gate, pre-access/effect evidence or terminal admission, deferred dispatch and packaged-source omission. Reuse already discriminating runs; identify the uncovered target explicitly | Observe the selected property and its positive. Redundant defenses may preserve the contract after a mutation; do not invent a stronger rule just to kill it. Missing-image-source is specifically a packaging test, not an authority/effect test. Unrelated setup/timeouts do not count |
| P06 — effective privileges; T02-13 | Complete the bidirectional runtime/reader/retained-route table plus OS immutable-file, arbitrary broker key and other-generation probes with existing allowed counterparts | Actual denial under the effective identity with an existing target; absence of a file alone is not permission enforcement |
| P07 — bounded evidence failure; T02-21 | Add the missing early-before-connect and evidence-export failure controls to the existing successful/stalled/paused cleanup observations | Bounded exit, incomplete evidence remains a failure, all owned resources reconciled and historical resources unchanged |
| P08 — final composition/review; T02-19 and implementation exit | Map all independently prepared families to actual producer evidence or an explicit unsupported gap; pin released checker for reproducible composition; complete affected foundation/ordinary-build evidence, required CI wiring and always-export inventory; hand off the exact final source | No planned/illustrative case counted as executed; source-linked evidence and reproducible commands; all required CI consumers remain mandatory. Budget/partition proposal uses measured work and awaits technical disposition before workflow change |

## Finite qualification: actual results and localized correction

The [coverage register](INC-03-T02-coverage.md) now identifies the finite runs,
all21 obligations and all53 independently specified variants. Those variants
are not53 fresh producer executions. Existing composed packets retain their
scope; separately exercised actual producers are labeled separately. The
independent R6 review closed its exact serial IC04/compatible and IC05/receipt
composition, not this complete implementation or R24.

### Original selection and the measured signal

The original neutrality run at04:37 failed the declared detector: absent
median11.577ms versus owned-record-only12.129ms, Cliff delta-0.3512 and p0.002.
Status/body/headers were equal and no original body was opened, but the
pre-authorization target selection differed. This was a real observed signal,
not a reason to rerun until green or relax the detector.

Only authority.ts, reception.ts and service.ts changed in the finite production
delta. Current treatment and existing exercise authority are resolved before an
original's protected target selection; the selector also restricts the current
scope and purpose. The established evaluator still handles descendants. Exact
target authority is rechecked at the existing later boundaries. The actual
descendant-only grant positive avoids silently requiring a root-level grant.

The corrected three-route comparison passed. Its+100ms shifted control detects
a signal; an actual-source mutation removing the new admission produces
protected selections and fails the independent zero-selection assertion. It
does not rely on a lucky nonsignificant timing rerun. These observations do not
promise universal timing indistinguishability.

### What the added controls actually establish

- Authority: invited exercise versus grant-only master; wrong existing scope,
  wrong purpose, missing immutable catalog, removed treatment, hidden JSON row,
  record-only original refusal and actual descendant grant.
- Finite resources: actual one-byte partial sender, idle and total deadlines,
  competing upload rejection with query progress, logical32-reception race,
  exactly64MiB of durable attempt reservations, and generation3 retained across
  a real process restart. The fourth cannot reset the counter. Durable reserved
  bytes are not a physical disk or memory measurement; HTTP declared-length
  rejection is not an application-stream overrun after admission.
- Effects: simultaneous same/different-key finalizers, existing wrong artifact,
  wrong generation, stale revision, failure before receipt commit, lost actual
  HTTP reply, cancellation ordering and prior-generation finalizer. Another
  legitimately invited principal uses the same key without sharing effects.
- COMMIT uncertainty: a captured-source fault pauses the actual PostgreSQL
  socket before receipt COMMIT, an independent connection observes receipt and
  evidence already committed, then the response socket is destroyed without
  resolving COMMIT to the caller. The same historical effect is recovered with
  no new capture; loading withdrawal and query withdrawal remain distinct.
- Delivery: all four governed reply consumers—profiles, reception, operation
  lookup, original—observe durable evidence, idle SQL, no client bytes, a real
  blocked source writer, transfer and subsequent writer completion. Writer-first
  and initially-expired counterparts refuse; restored valid positives succeed.
- Privileges: four effective SQL roles have allowed counterparts and bilateral
  refusals. The application runtime has no original-volume mount: stat, chmod,
  write and rename return ENOENT for the exact object independently verified in
  the broker. An authorized GET still returns its exact17bytes. This is mount
  isolation, not an absent test object or a claim of EACCES. A trusted broker's
  0400 object denial does not prove resistance to its owner changing permissions.
- Existing-fragment separation: real read_material invitation acceptance and
  EXCERPT delivery survive withdrawal of intake_original; the received original
  exists, was served as a positive, and its refused GET causes no object events.
  The existing fragment is NOT linked to that same original. T02 creates no such
  provenance relation, so the exact same-original variant remains unexercised.

### Directed mutations and retained diagnostic errors

| Property | Source fault / actual observation |
|---|---|
| Before application capture | Explicit one-byte read before admission caught by the oversized command observation; status alone still413 |
| Actual digest | Incremental check bypass survives: broker integrity still refuses400 after actual17-byte append, before format worker entry, with no receipt. The legitimate counterpart enters the format worker |
| Byte cap | Enlarged chunk changes actual at-limit consumption and is caught |
| Type acceptance | Skipped XLSX recognition is caught against actual wrong/malformed input |
| Generation/fence | Existing continuation-epoch and closed-before-open journal faults are caught at their named boundary; archived runs remain source-specific |
| Canonical payload / unique effect | Ignoring compatibility is caught; TX independently counts one receipt and job under concurrent finalizers |
| Sealed versus committed | Returning staged projection before the receipt transaction fails the actual receipt/job observation |
| Whole-original faculty | Bypassed original exercise/alive checks fail the record-only negative |
| Prior evidence / terminal admission | Omitted original delivery evidence and removed preselection admission each fail their own observed property |
| Deferred dispatch | Attempted dispatchable job is refused by the actual SQL constraint; no processing job is silently enabled |
| Packaged source | The actual built image omits authority.ts and Node fails module loading before runtime/SQL initialization |

The digest sequence retains three interpretations: the first surviving mutation,
an intermediate assertion that wrongly demanded no broker integrity read, and
the corrected check that demands no format-worker entry. No new no-read product
requirement was adopted. The intermediate failure is not evidence of a contract
violation. The final paired run preserves400/no receipt/17 appended bytes/zero
format entries even without the early digest check; zero surviving mutations is
not an acceptance rule.

The package mutation's first wrapper expected the parent test title, but the
real failure occurred during module loading and named the test file. Its failed
wrapper remains unchanged. A read-only reanalysis checks the exact missing
module, source mutation and all12 released resources. It does not create another
producer execution. Early-before-connect and evidence-export controls also
retain failing child status and complete owned cleanup; remaining exports are
attempted after one export fails.

## Mandatory CI composition and before/after correspondence

The existing workflow keeps triggers, concurrency and check/db/app unchanged.
The required reading context is now a result-only aggregator over four explicit
jobs. It runs its result check after failure and approves only four success
values with no workflow cancellation. Missing, unknown, failed, cancelled or
skipped results are not successful. It does not fetch another runner's files or
repeat its tests. This follows the explicit dependency/always semantics in the
[GitHub workflow reference](https://docs.github.com/en/actions/reference/workflows-and-actions/workflow-syntax#jobsjob_idneeds).

| Previous required work or artifact | Current mandatory destination |
|---|---|
| Reading/access boundaries, contract/unit suites and access transport experiment | reading-foundations, unchanged npm commands |
| Access runtime, invitations, authenticated reading, administration and journey Docker suites | intake-reception-recovery, actual five-suite source-fence composition |
| Packaging check embedded in test:access:journey | reading-foundations, explicit node --test tests/access/test_runtime_packaging.mjs |
| All six T01 groups | reading-foundations, unchanged |
| Reading PG service, app types, ordinary build, HTTP, Chromium installation, browser and integration | reading-foundations; core typecheck also explicit |
| Earlier seven ordinary T02 groups | intake-reception-behavior, unchanged scenario arguments |
| Twelve finite groups plus receipt-COMMIT loss | intake-reception-behavior, explicit scenarios in t02_plan.mjs |
| Thirteen recovery scenarios | intake-reception-recovery, unchanged arguments plus the five-suite coupled group |
| Ten producer fault recipes; pre-capture/epoch/journal guard recipes; two lifecycle/export failures | intake-reception-mutations, paired positives and bounded expected-failure classifiers |
| intake-foundation-evidence and reading-integration-screenshots | reading-foundations, existing always exports |
| Five access evidence directories, including14 existing synthetic PNGs | Coupled-run directories under intake-reception-recovery-evidence, always exported |
| T02 behavior, recovery and mutation evidence, including failed runs | Each producing job prepares and always exports its own uniquely named artifact; missing/incomplete export fails |

The previous workflow and script fixtures retain the before inventory. A YAML
parser bundled by the already-pinned Playwright1.63.0 reads the actual workflow;
independent required lists check the actual executed plans and the retained npm
definitions, rather than deriving expectations from a shortened current list.
Eight local tests cover four-success aggregation, each failure/cancellation/
skip/absence, removal/emptying/conditional skipping of jobs, removal of every
scenario/command/artifact, duplicate YAML keys and the real evidence exporter.
The source-specific guard classifier was also checked against three archived
actual mutations and against deliberately unrelated failure names. That is
classifier replay, not a new Docker execution.

Public artifacts preserve structural JSON with credential-bearing fields and
raw command payloads omitted, plus existing synthetic browser PNGs unchanged.
Their manifest distinguishes raw versus public hashes, omissions and incomplete
copies. Raw local records remain intact. This is not a universal secret scanner
or a claim that sanitized JSON is byte-identical raw evidence. Actual export was
tested against reception, all five coupled access outputs and an export-failure
run; the paired unit input also proves incomplete JSON cannot produce a complete
export. No source archives, credentials, control backups or launch config are
uploaded by this new exporter.

### Measurement and limits

The local nonduplicated foundation measurement retained29 successful steps,
then failed before any browser scenario because the selected headless executable
was absent. The existing installed-Chrome option subsequently passed8/8 and
integration18/18 without changing tests. The default Windows bash invocation
then lacked Node; explicit Git Bash/Node22 completed check.sh. Both failed
measurement wrappers remain failed historical records. These are separately
identified invocations, not a single green matrix. Useful foundation work is
approximately394seconds plus the separately captured repository check; no cold
install time is included.

Recovery13 plus coupled access measured655.988seconds. Behavior is approximately
12minutes by existing component measurements. The mutation job adds paired
baselines and the preserved guard checks: allow roughly10–14minutes locally,
not a remote guarantee. Each producer job remains bounded to20minutes. These
are PER-JOB limits: four concurrent runners can reduce waiting but increase
concurrent and total runner budget. There is no claim that total budget stayed
at the old single-job20minutes. A future cold Ubuntu job that exceeds its bound
must fail and be diagnosed; no automatic timeout increase or retry-until-green.

No remote CI was invoked. The local change affects wiring, evidence packaging
and tests; no new product behavior was added after the finite production fix.
The next technical review consumes exact source/run manifests. R24's four actual
violations, BROWSER-I03/J19, real-data prohibition and same-original fragment
lineage limitation remain declared. This is not acceptance of the ADR or T02.

The measured 14m16s is T02's local matrix only. The earlier approximately
nine-minute foundation/reading run is a historical planning reference with
overlapping access tests, not a current additive total. Neither their simple
sum nor this local result proves a remote-CI budget. No timeout was raised and
the workflow was not repartitioned by this measurement.

The four temporal counterexamples are a separate **scope disposition**, not
another passing checkbox: the approved experiment expressly measures writer-free
expiry, but the new reservation/capture/receipt/original effects must be qualified
against that scope. Their acceptance is not inferred merely from R24 being open.
They remain `observed-violation` while that determination is recorded. Additional
fuzz campaigns, new formats, a generic observer redesign and universal timing or
denial-of-service claims are not required to close this bounded test package.

R24 remains an observed writer-free temporal violation. BROWSER-I03 and
BROWSER-J19 remain open with their existing diagnostic criteria. No passing run
closes them. Full temporal conformity, real data, production deployment and T03
execution remain outside this delivery's authorized experimental scope.

## Bridge composition and post-handoff observation correction

The latest local correction and exact run addresses are in
[the coverage register](INC-03-T02-coverage.md#local-bridge-and-post-handoff-correction-2026-09-13).
Earlier measurements in this document retain their original source and scope.

The bridge now derives observation admission and participant selection from
ci/intake/reception/admin_observation.mjs. The eight FENCE consumers receive only
the two existing diagnostic reads needed by their shared digest test. Their
fault-injection permissions, owned resource selection and output limits do not
expand. tests/intake/t02/test_admin_observation.mjs reads the actual requests,
checks independent consumers and detects removal of either permission.

The only production runtime change in this correction is the intake terminal's
post-handoff observation error path; service.ts extends the diagnostic type.
SQL, authority, receipt construction, canonicalization, private storage and
fencing implementations are unchanged. A failed observation cannot undo bytes
already handed off. It reports safely through the installed synchronous receiver
or application stderr when that receiver is absent/throws, then releases rather
than attempting a second response. No database compensating write, global
shutdown policy, retry or wait for eventual browser closure was added.

The delivered composition's existing hook already reported the SQL error.
The earlier absolute claim of silence was too broad. The retained failing run
instead establishes the precise absent/throwing-receiver gap. The new runtime
scenario causes a real SQL INSERT failure after res.end, with an identical
successful control and independent SQL/client/writer observations. Test-only
IPC configures the child receiver; it is not a public service operation.

Reproduce sequentially with the existing Node22, pinned container images and
synthetic runtime prerequisites:

```text
node --test tests/intake/t02/test_bridge.mjs tests/intake/t02/test_request_observer.mjs tests/intake/t02/test_ci_wiring.mjs
node ci/intake_t02_check.mjs --group delivery-order
node ci/intake_t02_check.mjs --group transactions
node ci/intake_t02_check.mjs --group transactions --receipt-commit-loss
node ci/intake_t02_check.mjs --group fencing-probe
node ci/intake_t02_matrix.mjs --suite recovery
node ci/intake_t02_check.mjs --group delivery-order --observation-mutation drop-fallback
node ci/intake_t02_check.mjs --group delivery-order --observation-mutation destroy-after-observation
```

The last two commands intentionally return failure; require the specific
post-handoff scenario failures, one changed captured terminal source and complete
cleanup. Do not classify any nonzero exit as a detected mutation. Each invocation
retains its source/command/resource manifest and failed records. The normal
delivery-order group already belongs to mandatory behavior verification, so the
new SQL experiment is wired without widening CI to another broad campaign.
The two new captured-source mutants are reproducible supplementary controls;
the prior ten-mutant mandatory plan is unchanged.

The controlled probe proves delivery, SQL failure, the actual diagnostic
recipient and release separately. It does not promise an infallible external log
sink or physical receipt by every client. No acceptance or publication follows
from these passing runs. The ADR's T02 amendment remains proposed; its earlier
T01 foundation decision remains accepted.
